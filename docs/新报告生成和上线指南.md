# YECO 新报告生成和上线指南

> **文档性质**: 系统规范（宪法级别）
> **版本**: 1.0
> **更新日期**: 2025-11-13
> **适用对象**: Claude Code AI 及所有参与报告生成的开发者

---

## 📋 目录

1. [系统架构概述](#系统架构概述)
2. [报告生成完整流程](#报告生成完整流程)
3. [目录结构规范](#目录结构规范)
4. [HTML文件规范](#html文件规范)
5. [翻译文件规范](#翻译文件规范)
6. [JavaScript文件规范](#javascript文件规范)
7. [样式文件规范](#样式文件规范)
8. [图片处理规范](#图片处理规范)
9. [报告索引更新规范](#报告索引更新规范)
10. [测试与上线清单](#测试与上线清单)
11. [常见错误与排查](#常见错误与排查)

---

## 系统架构概述

### 核心理念

YECO 网站采用**组件化 + 多语言**架构，新报告必须完美融入现有系统：

```
系统层次结构：
├── 全局系统层
│   ├── i18n 多语言核心（window.i18n）
│   ├── RTL 处理器（window.rtlHandler）
│   └── 图片优化器（window.YECOImageOptimizer）
├── 共享组件层
│   ├── Navigation（导航栏）
│   └── Footer（页脚）
└── 页面层
    ├── 首页（data-i18n 模式）
    ├── 趋势页（data-i18n 模式）
    └── 报告页（手动更新模式）← 你要生成的内容
```

### 关键约定

⚠️ **这些规则不可违反**：

1. **脚本加载顺序**：严格按照 [HTML文件规范](#html文件规范) 中的顺序加载
2. **路径约定**：报告必须放在 `pages/reports/{year}-{month}-{region}/`
3. **多语言支持**：所有文本内容必须提供 zh-CN、en-US、ar-SA 三种语言
4. **图片路径**：所有图片必须放在报告目录的 `images/` 子目录
5. **命名空间**：翻译变量必须导出到 `window.zhCN`、`window.enUS`、`window.arSA`

---

## 报告生成完整流程

### 阶段 1: 准备工作

**输入**：用户提供的文字版报告内容

**你需要做的**：

1. 确定报告元数据：
   - 年份-月份（如 `2025-10`）
   - 地区标识（如 `middleeast`）
   - 报告ID：`{year}-{month}-{region}`（如 `2025-10-middleeast`）

2. 创建目录：`pages/reports/{reportId}/`

3. 分析报告结构，识别章节和内容块

### 阶段 2: 生成文件

**按以下顺序生成**：

1. ✅ `i18n/zh-CN.js` - 中文翻译（基于用户提供的内容）
2. ✅ `i18n/en-US.js` - 英文翻译
3. ✅ `i18n/ar-SA.js` - 阿拉伯文翻译
4. ✅ `index.html` - 报告主页面
5. ✅ `script.js` - 交互逻辑和i18n集成
6. ✅ `style.css` - 报告样式
7. ✅ `images/` - 图片占位符（如果用户提供图片）

### 阶段 3: 更新系统索引

**关键步骤**：

1. ✅ 读取 `pages/trends/data/reports-index.json`
2. ✅ 在 `reports` 数组开头插入新报告数据
3. ✅ 保存文件

### 阶段 4: 优化和测试

**执行命令**：

```bash
# 优化报告图片
npm run optimize-images:report

# 本地测试
npm run dev
```

**测试清单**：见 [测试与上线清单](#测试与上线清单)

---

## 目录结构规范

### 标准目录结构

```
pages/reports/{year}-{month}-{region}/
├── index.html              # 报告HTML（必需）
├── script.js               # 报告脚本（必需）
├── style.css               # 报告样式（必需）
├── i18n/                   # 翻译目录（必需）
│   ├── zh-CN.js           # 中文翻译（必需）
│   ├── en-US.js           # 英文翻译（必需）
│   └── ar-SA.js           # 阿拉伯文翻译（必需）
└── images/                 # 图片目录（必需）
    ├── 01主视觉.png       # 原始图片
    ├── 02xxx.png
    └── optimized/          # 优化后图片（自动生成，勿手动创建）
        ├── 01主视觉.webp
        ├── 01主视觉-small.webp
        ├── 01主视觉-medium.webp
        └── ...
```

### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 报告目录 | `{year}-{month}-{region}` | `2025-11-middleeast` |
| 地区标识 | 小写英文 | `middleeast`, `asia`, `europe` |
| 图片文件 | 中文描述性命名 | `01主视觉.png`, `02设计案例.jpg` |

---

## HTML文件规范

### 完整模板结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YECO - {报告标题}</title>

    <!-- 全局样式（顺序不可变！） -->
    <link rel="stylesheet" href="../../../assets/styles/variables.css">
    <link rel="stylesheet" href="../../../assets/styles/typography.css">
    <link rel="stylesheet" href="../../../assets/styles/rtl.css">

    <!-- 组件样式 -->
    <link rel="stylesheet" href="../../../shared/components/Navigation/styles.css">
    <link rel="stylesheet" href="../../../shared/components/Footer/styles.css">

    <!-- 页面样式 -->
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <!-- ==================== 必需的占位符 ==================== -->
    <!-- 导航栏占位符（必须在页面最顶部） -->
    <div id="navigation-placeholder"></div>

    <!-- ==================== 报告内容区域 ==================== -->
    <!-- 你的报告内容写在这里 -->

    <!-- 示例：封面 -->
    <section class="cover-page" style="--cover-bg-image: url('images/01主视觉.png');">
        <div class="cover-content">
            <div class="cover-logo">YECO</div>
            <div class="cover-title">香水行业趋势月刊</div>
            <div class="cover-subtitle">解码香水消费者的灵魂</div>
            <!-- 更多内容... -->
        </div>
    </section>

    <!-- 示例：目录 -->
    <section class="table-of-contents">
        <div class="container">
            <h2 class="toc-title">本期内容</h2>
            <!-- 更多内容... -->
        </div>
    </section>

    <!-- 更多章节... -->

    <!-- ==================== 必需的占位符 ==================== -->
    <!-- 页脚占位符（必须在页面最底部） -->
    <div id="footer-placeholder"></div>

    <!-- ==================== 脚本加载（顺序严格！） ==================== -->
    <!-- 0. 图片优化加载器（最先加载） -->
    <script src="../../../shared/utils/image-optimizer-loader.js"></script>

    <!-- 1. i18n核心系统 -->
    <script src="../../../shared/i18n/i18n-core.js"></script>
    <script src="../../../shared/i18n/rtl-handler.js"></script>

    <!-- 2. 共享组件翻译数据 -->
    <script src="../../../shared/components/Navigation/i18n.js"></script>

    <!-- 3. 页面翻译数据 -->
    <script src="i18n/zh-CN.js"></script>
    <script src="i18n/en-US.js"></script>
    <script src="i18n/ar-SA.js"></script>

    <!-- 4. 共享组件脚本 -->
    <script src="../../../shared/components/Navigation/script.js"></script>
    <script src="../../../shared/components/Footer/script.js"></script>

    <!-- 5. 页面脚本（最后加载） -->
    <script src="script.js"></script>
</body>
</html>
```

### ⚠️ 关键注意事项

1. **脚本加载顺序**：
   - 图片优化器 → i18n核心 → 组件翻译 → 页面翻译 → 组件脚本 → 页面脚本
   - **任何顺序错误都会导致翻译失败**

2. **占位符ID**：
   - 导航栏：`id="navigation-placeholder"`
   - 页脚：`id="footer-placeholder"`
   - **ID不可更改，否则组件无法加载**

3. **样式引用顺序**：
   - variables → typography → rtl → Navigation → Footer → 页面样式
   - **顺序错误会导致样式冲突**

4. **相对路径**：
   - 从报告目录到根目录：`../../../`
   - **路径错误会导致资源加载失败**

---

## 翻译文件规范

### zh-CN.js 文件结构

```javascript
// 中文语言包
const zhCN = {
    // ==================== 页面元数据 ====================
    pageTitle: "YECO - 香水行业趋势{月份}月刊 - {地区}版",

    // ==================== 封面 ====================
    cover: {
        logo: "YECO",
        title: "香水行业趋势月刊",
        subtitle: "解码香水消费者的灵魂",
        subtitleEn: "Decoding the Soul of Perfume Consumers",
        issueNumber: "第01期",
        issueDate: "2025年10月",
        issueRegion: "中东版"
    },

    // ==================== 目录 ====================
    toc: {
        title: "本期内容",
        items: [
            { number: "01", title: "执行摘要", desc: "香水作为【图腾物件】的市场转变" },
            { number: "02", title: "设计美学分析", desc: "渴望的形态与美学的战场" },
            // 更多目录项...
        ]
    },

    // ==================== 各章节内容 ====================
    executive: {
        sectionNumber: "01",
        sectionTitle: "摘要",
        featureTitle: "香水作为【图腾物件】",
        featureText: "本报告基于对Reddit香水社区的深度定性分析...",
        // 更多字段...
    },

    // ==================== 图片占位符提示 ====================
    placeholders: {
        coverImage: "封面主视觉",
        coverImageSize: "建议尺寸: 1920x1080px",
        // 更多占位符...
    }
};

// ⚠️ 必须导出到 window（这一行不可省略！）
window.zhCN = zhCN;
```

### en-US.js 文件结构

```javascript
// English language package
const enUS = {
    // Page metadata
    pageTitle: "YECO - Perfume Industry Trend {Month} Monthly - {Region} Edition",

    // Cover
    cover: {
        logo: "YECO",
        title: "Perfume Industry Trend Monthly",
        subtitle: "Decoding the Soul of Perfume Consumers",
        subtitleEn: "Decoding the Soul of Perfume Consumers",
        issueNumber: "Issue 01",
        issueDate: "October 2025",
        issueRegion: "Middle East Edition"
    },

    // Table of contents
    toc: {
        title: "Contents",
        items: [
            { number: "01", title: "Executive Summary", desc: "Perfume Bottles as Totemic Objects" },
            { number: "02", title: "Design Aesthetics Analysis", desc: "The Form of Desire and Aesthetic Battlefield" },
            // More items...
        ]
    },

    // Section content
    executive: {
        sectionNumber: "01",
        sectionTitle: "Summary",
        featureTitle: "Perfume as a Totemic Object",
        featureText: "Based on in-depth qualitative analysis of Reddit perfume communities...",
        // More fields...
    },

    // Image placeholders
    placeholders: {
        coverImage: "Cover Visual",
        coverImageSize: "Suggested size: 1920x1080px",
        // More placeholders...
    }
};

// ⚠️ Must export to window (this line cannot be omitted!)
window.enUS = enUS;
```

### ar-SA.js 文件结构

```javascript
// حزمة اللغة العربية
const arSA = {
    // بيانات تعريف الصفحة
    pageTitle: "YECO - تقرير اتجاهات صناعة العطور {الشهر} الشهري - نسخة {المنطقة}",

    // الغلاف
    cover: {
        logo: "YECO",
        title: "تقرير اتجاهات صناعة العطور الشهري",
        subtitle: "فك شفرة روح مستهلكي العطور",
        subtitleEn: "Decoding the Soul of Perfume Consumers",
        issueNumber: "العدد 01",
        issueDate: "أكتوبر 2025",
        issueRegion: "نسخة الشرق الأوسط"
    },

    // جدول المحتويات
    toc: {
        title: "محتويات هذا العدد",
        items: [
            { number: "01", title: "الملخص التنفيذي", desc: "زجاجات العطور كأشياء طوطمية" },
            { number: "02", title: "تحليل الجماليات التصميمية", desc: "شكل الرغبة وساحة الجمال" },
            // المزيد من العناصر...
        ]
    },

    // محتوى الأقسام
    executive: {
        sectionNumber: "01",
        sectionTitle: "ملخص",
        featureTitle: "العطر كشيء طوطمي",
        featureText: "بناءً على التحليل النوعي المتعمق لمجتمعات العطور في Reddit...",
        // المزيد من الحقول...
    },

    // عناصر نائبة للصور
    placeholders: {
        coverImage: "الصورة المرئية للغلاف",
        coverImageSize: "الحجم المقترح: 1920x1080 بكسل",
        // المزيد من العناصر النائبة...
    }
};

// ⚠️ يجب التصدير إلى window (لا يمكن حذف هذا السطر!)
window.arSA = arSA;
```

### 翻译数据结构原则

1. **嵌套对象**：使用嵌套对象组织复杂内容，避免平铺
2. **一致性**：三个语言文件的对象结构必须完全一致
3. **键名约定**：
   - 使用驼峰命名（camelCase）
   - 章节用名词（如 `executive`, `design`, `narrative`）
   - 元素用描述性名称（如 `title`, `desc`, `text`）
4. **特殊内容**：
   - HTML标签使用 `innerHTML` 字段
   - 高亮文本用 `highlight` 字段单独存储

---

## JavaScript文件规范

### script.js 完整模板

```javascript
// ==========================================
// YECO {报告名称} - 交互脚本
// ==========================================

document.addEventListener('DOMContentLoaded', function() {

    // ==========================================
    // 滚动动画 - Intersection Observer
    // ==========================================
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const fadeInObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
                fadeInObserver.unobserve(entry.target);
            }
        });
    }, observerOptions);

    document.querySelectorAll('.fade-in').forEach(el => {
        fadeInObserver.observe(el);
    });

    // ==========================================
    // 统计数字动画
    // ==========================================
    function animateValue(element, start, end, duration, suffix = '') {
        const range = end - start;
        const increment = range / (duration / 16);
        let current = start;

        const timer = setInterval(() => {
            current += increment;
            if ((increment > 0 && current >= end) || (increment < 0 && current <= end)) {
                current = end;
                clearInterval(timer);
            }

            if (suffix === '$') {
                element.textContent = Math.floor(current) + suffix;
            } else {
                element.textContent = Math.floor(current) + suffix;
            }
        }, 16);
    }

    const statObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const element = entry.target;
                const text = element.textContent.trim();

                let value, suffix = '';
                if (text.includes('$')) {
                    value = parseInt(text.replace('$', ''));
                    suffix = '$';
                } else if (text.includes('%')) {
                    value = parseInt(text.replace('%', ''));
                    suffix = '%';
                } else {
                    value = parseInt(text);
                }

                if (!isNaN(value)) {
                    element.textContent = '0' + suffix;
                    animateValue(element, 0, value, 2000, suffix);
                }

                statObserver.unobserve(entry.target);
            }
        });
    }, observerOptions);

    document.querySelectorAll('.stat-icon, .stat-number').forEach(el => {
        statObserver.observe(el);
    });

    // ==========================================
    // 平滑滚动
    // ==========================================
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const targetId = this.getAttribute('href');
            if (targetId === '#') return;

            const targetElement = document.querySelector(targetId);
            if (targetElement) {
                targetElement.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });

    // ==========================================
    // 进度指示器（滚动进度）
    // ==========================================
    function createProgressBar() {
        const progressBar = document.createElement('div');
        progressBar.style.cssText = `
            position: fixed;
            top: 0;
            left: 0;
            width: 0%;
            height: 3px;
            background: linear-gradient(90deg, var(--accent-gold), var(--soft-gold));
            z-index: 9999;
            transition: width 0.1s ease;
        `;
        document.body.appendChild(progressBar);

        window.addEventListener('scroll', () => {
            const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
            const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (winScroll / height) * 100;
            progressBar.style.width = scrolled + '%';
        });
    }

    createProgressBar();

    // 你可以在这里添加更多交互功能...

});

// ==========================================
// 国际化 (i18n) 功能 - 集成共享 i18n 系统
// ==========================================

// 获取合并的翻译数据
function getI18nData() {
    // 合并报告页翻译 + 导航栏翻译 + 页脚翻译
    const reportData = {
        'zh-CN': window.zhCN || {},
        'en-US': window.enUS || {},
        'ar-SA': window.arSA || {}
    };

    // 合并导航栏翻译数据
    if (window.navigationI18n) {
        Object.keys(reportData).forEach(lang => {
            if (window.navigationI18n[lang]) {
                reportData[lang] = {
                    ...reportData[lang],
                    ...window.navigationI18n[lang]
                };
            }
        });
    }

    // 合并页脚翻译数据
    if (window.footerI18n) {
        Object.keys(reportData).forEach(lang => {
            if (window.footerI18n[lang]) {
                reportData[lang] = {
                    ...reportData[lang],
                    ...window.footerI18n[lang]
                };
            }
        });
    }

    return reportData;
}

// 更新页面文本内容
function updatePageContent(lang) {
    const i18nData = getI18nData();
    const data = i18nData[lang];
    if (!data) {
        console.error('语言包未找到:', lang);
        return;
    }

    // 更新页面标题
    document.title = data.pageTitle;
    document.documentElement.lang = lang;

    // ==================== 更新封面 ====================
    updateElement('.cover-logo', data.cover.logo);
    updateElement('.cover-title', data.cover.title);
    updateElement('.cover-subtitle', data.cover.subtitle);
    updateElement('.cover-subtitle-en', data.cover.subtitleEn);
    updateElement('.issue-number', data.cover.issueNumber);
    updateElement('.issue-date', data.cover.issueDate);
    updateElement('.issue-region', data.cover.issueRegion);

    // ==================== 更新目录 ====================
    updateElement('.toc-title', data.toc.title);
    const tocItems = document.querySelectorAll('.toc-item');
    tocItems.forEach((item, index) => {
        if (data.toc.items[index]) {
            const title = item.querySelector('h3');
            const desc = item.querySelector('p');
            if (title) title.textContent = data.toc.items[index].title;
            if (desc) desc.textContent = data.toc.items[index].desc;
        }
    });

    // ==================== 更新其他章节 ====================
    // 根据你的报告结构添加更多updateElement调用
    // 示例：
    // updateSectionTitle('.executive-summary', data.executive.sectionNumber, data.executive.sectionTitle);
    // updateElement('.feature-title', data.executive.featureTitle);
    // ... 等等

    console.log('✅ 页面内容已更新为:', lang);
}

// 辅助函数：更新单个元素
function updateElement(selector, value) {
    const element = document.querySelector(selector);
    if (element && value !== undefined) {
        element.textContent = value;
    }
}

// 辅助函数：更新章节标题
function updateSectionTitle(sectionSelector, number, title) {
    const section = document.querySelector(sectionSelector);
    if (!section) return;

    const numberEl = section.querySelector('.section-number');
    const titleEl = section.querySelector('.section-title');

    if (numberEl) numberEl.textContent = number;
    if (titleEl) titleEl.textContent = title;
}

// ==========================================
// 报告页 i18n 初始化 - 使用共享 i18n 系统
// ==========================================
(function initReportI18n() {
    console.log('📊 报告页面: 开始初始化 i18n...');

    let retryCount = 0;
    const maxRetries = 50; // 最多重试 5 秒

    // 等待 i18n 系统和导航组件完全初始化
    function setupReportLanguage() {
        retryCount++;

        // 检查 i18n 系统是否存在
        if (!window.i18n) {
            if (retryCount < maxRetries) {
                console.log(`⏳ 等待 i18n 系统加载... (${retryCount}/${maxRetries})`);
                setTimeout(setupReportLanguage, 100);
            } else {
                console.error('❌ i18n 系统加载超时，使用默认语言');
                updatePageContent('zh-CN');
            }
            return;
        }

        // 检查 i18n 系统是否已初始化
        if (!window.i18n.initialized) {
            if (retryCount === 1) {
                // 第一次检测到未初始化，主动初始化
                console.log('🔧 i18n 系统未初始化，开始主动初始化...');

                // 获取报告页面的翻译数据
                const i18nData = getI18nData();

                // 调用 init() 方法初始化 i18n 系统
                window.i18n.init(i18nData).then(() => {
                    console.log('✅ i18n 系统初始化成功');
                    // 初始化完成后继续设置
                    setTimeout(setupReportLanguage, 100);
                }).catch(error => {
                    console.error('❌ i18n 初始化失败:', error);
                    updatePageContent('zh-CN');
                });
                return;
            }

            if (retryCount < maxRetries) {
                console.log(`⏳ 等待 i18n 系统初始化... (${retryCount}/${maxRetries})`);
                setTimeout(setupReportLanguage, 100);
            } else {
                console.error('❌ i18n 系统初始化超时');
                updatePageContent('zh-CN');
            }
            return;
        }

        console.log('✅ 检测到共享 i18n 系统');
        console.log('📍 当前语言:', window.i18n.currentLang);

        // 监听全局语言切换事件
        window.i18n.on('languageChanged', (data) => {
            console.log('📊 报告页面: 收到语言切换事件:', data.to);
            updatePageContent(data.to);
        });

        // 使用 i18n 系统的当前语言初始化页面
        const currentLang = window.i18n.currentLang || 'zh-CN';
        console.log('📊 报告页面: 使用语言:', currentLang);
        updatePageContent(currentLang);

        console.log('✅ 报告页面 i18n 初始化完成');
    }

    // 等待DOM加载完成后再初始化
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', () => {
            console.log('📄 DOM 加载完成，开始设置报告页语言');
            setTimeout(setupReportLanguage, 300); // 给导航组件时间初始化
        });
    } else {
        console.log('📄 DOM 已加载，延迟设置报告页语言');
        setTimeout(setupReportLanguage, 300);
    }
})();
```

### ⚠️ 关键注意事项

1. **getI18nData() 函数**：
   - 必须合并三个翻译源：报告、导航、页脚
   - 使用扩展运算符 `{...}`
   - 不可省略任何一个合并步骤

2. **updatePageContent() 函数**：
   - 必须更新所有文本内容
   - 根据你的报告结构添加相应的更新逻辑
   - 每个章节都要有对应的更新代码

3. **initReportI18n() IIFE**：
   - 不可修改核心逻辑
   - 重试机制是必需的
   - 延迟300ms是为了等待组件初始化

---

## 样式文件规范

### style.css 基本结构

```css
/* ==========================================
   YECO {报告名称} - 样式
   ========================================== */

/* ==========================================
   全局变量和重置
   ========================================== */
:root {
    /* 报告特定颜色（如果有） */
    --report-primary: #...;
    --report-accent: #...;
}

/* ==========================================
   封面样式
   ========================================== */
.cover-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    background: var(--primary-black);
    /* 背景图片通过内联样式设置 */
    background-size: cover;
    background-position: center;
}

.cover-content {
    text-align: center;
    z-index: 2;
}

.cover-logo {
    font-family: var(--font-display);
    font-size: var(--text-4xl);
    font-weight: 700;
    background: linear-gradient(135deg, var(--accent-gold), var(--soft-gold));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--spacing-lg);
}

/* ==========================================
   目录样式
   ========================================== */
.table-of-contents {
    padding: var(--spacing-xxl) 0;
    background: var(--primary-black);
}

.toc-title {
    text-align: center;
    font-size: var(--text-3xl);
    color: var(--accent-gold);
    margin-bottom: var(--spacing-xl);
}

.toc-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-lg);
    max-width: var(--container-xl);
    margin: 0 auto;
    padding: 0 var(--spacing-xl);
}

.toc-item {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(212, 175, 55, 0.2);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    cursor: pointer;
    transition: all var(--transition-base);
}

.toc-item:hover {
    transform: translateY(-5px);
    border-color: var(--accent-gold);
    box-shadow: 0 8px 30px rgba(212, 175, 55, 0.2);
}

/* ==========================================
   章节通用样式
   ========================================== */
section {
    padding: var(--spacing-xxl) 0;
}

.container {
    max-width: var(--container-lg);
    margin: 0 auto;
    padding: 0 var(--spacing-xl);
}

.section-header {
    text-align: center;
    margin-bottom: var(--spacing-xl);
}

.section-number {
    font-size: var(--text-sm);
    color: var(--accent-gold);
    font-weight: 600;
    letter-spacing: 2px;
}

.section-title {
    font-size: var(--text-3xl);
    color: var(--white);
    margin-top: var(--spacing-sm);
}

/* ==========================================
   滚动动画
   ========================================== */
.fade-in {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease, transform 0.6s ease;
}

.fade-in.visible {
    opacity: 1;
    transform: translateY(0);
}

/* ==========================================
   响应式设计
   ========================================== */
@media (max-width: 768px) {
    .cover-logo {
        font-size: var(--text-3xl);
    }

    .toc-grid {
        grid-template-columns: 1fr;
    }

    .section-title {
        font-size: var(--text-2xl);
    }
}

/* ==========================================
   RTL 支持
   ========================================== */
[dir="rtl"] .toc-grid,
[dir="rtl"] .container {
    direction: rtl;
}

/* ==========================================
   打印样式
   ========================================== */
@media print {
    .fade-in {
        opacity: 1 !important;
        transform: none !important;
    }
}
```

### 样式编写原则

1. **使用CSS变量**：优先使用 `var(--xxx)` 而非硬编码值
2. **响应式优先**：所有布局必须适配移动端
3. **RTL支持**：为阿拉伯语添加 `[dir="rtl"]` 样式
4. **动画性能**：使用 `transform` 和 `opacity`，避免 `width`/`height` 动画
5. **注释清晰**：每个区块添加注释分隔

---

## 图片处理规范

### 图片命名和存放

1. **命名规则**：
   ```
   01主视觉.png
   02设计案例.jpg
   03数据图表.png
   ```
   - 使用序号 + 中文描述
   - 支持格式：PNG、JPG、JPEG

2. **存放位置**：
   ```
   pages/reports/{reportId}/images/
   ├── 01主视觉.png
   ├── 02xxx.png
   └── ...
   ```

3. **⚠️ 禁止**：
   - 不要手动创建 `optimized/` 目录
   - 不要直接放置 WebP 格式图片
   - 不要放在其他目录（优化器只处理 `images/` 目录）

### HTML中引用图片

```html
<!-- ✅ 正确：使用原始路径 -->
<img src="images/01主视觉.png" alt="封面主视觉">

<!-- ✅ 正确：CSS背景图 -->
<div style="--cover-bg-image: url('images/01主视觉.png');"></div>

<!-- ❌ 错误：不要手动指定WebP -->
<img src="images/optimized/01主视觉.webp" alt="封面主视觉">
```

### 图片优化流程

**完成报告后执行**：

```bash
# 优化报告图片
npm run optimize-images:report
```

**自动发生的事情**：
1. 扫描 `pages/reports/` 下所有 `images/` 目录
2. 为每张图片生成：
   - `xxx.webp` (原始尺寸)
   - `xxx-small.webp` (≤640px)
   - `xxx-medium.webp` (≤1024px)
   - `xxx-large.webp` (原始尺寸)
3. 保存到 `images/optimized/` 目录

**浏览器自动优化**：
- `image-optimizer-loader.js` 自动将 `<img src="images/xxx.png">` 替换为最佳WebP版本
- 不支持WebP的浏览器自动回退到原图
- 自动添加 `loading="lazy"`

---

## 报告索引更新规范

### 读取现有索引

```javascript
// 读取文件
const indexPath = 'pages/trends/data/reports-index.json';
const indexData = JSON.parse(fs.readFileSync(indexPath, 'utf-8'));
```

### 生成新报告数据

```javascript
const newReport = {
    "id": "{year}-{month}-{region}",  // 如 "2025-11-middleeast"
    "date": "{year}-{month}",          // 如 "2025-11"
    "year": 2025,                      // 数字类型
    "month": 11,                       // 数字类型
    "region": "{region}",              // 如 "middleeast", "asia", "europe"
    "title": {
        "zh-CN": "香水行业趋势月刊 - {地区}版",
        "en-US": "Perfume Industry Trend Monthly - {Region} Edition",
        "ar-SA": "تقرير اتجاهات صناعة العطور الشهري - نسخة {المنطقة}"
    },
    "subtitle": {
        "zh-CN": "{副标题}",
        "en-US": "{Subtitle}",
        "ar-SA": "{العنوان الفرعي}"
    },
    "description": {
        "zh-CN": "{简短描述，1-2句话，介绍报告核心内容}",
        "en-US": "{Brief description in English}",
        "ar-SA": "{وصف موجز باللغة العربية}"
    },
    "tags": {
        "zh-CN": ["标签1", "标签2", "标签3", "标签4", "标签5"],
        "en-US": ["Tag1", "Tag2", "Tag3", "Tag4", "Tag5"],
        "ar-SA": ["علامة1", "علامة2", "علامة3", "علامة4", "علامة5"]
    },
    "coverImage": "../../pages/reports/{reportId}/images/01主视觉.png",
    "url": "../../pages/reports/{reportId}/",
    "featured": true,    // 是否精选（首个报告通常为true）
    "published": true    // 是否已发布
};
```

### 插入到索引

```javascript
// ⚠️ 在数组开头插入（最新的报告在前面）
indexData.reports.unshift(newReport);

// 保存文件
fs.writeFileSync(indexPath, JSON.stringify(indexData, null, 2), 'utf-8');
```

### ⚠️ 关键注意事项

1. **路径格式**：
   - `coverImage`: `../../pages/reports/{reportId}/images/01主视觉.png`
   - `url`: `../../pages/reports/{reportId}/`
   - 两个 `../` 是从趋势页到根目录的相对路径

2. **地区标识映射**：
   ```
   middleeast  → 中东 / Middle East / الشرق الأوسط
   asia        → 亚洲 / Asia / آسيا
   europe      → 欧洲 / Europe / أوروبا
   northamerica → 北美 / North America / أمريكا الشمالية
   ```

3. **标签数量**：建议 3-5 个标签

4. **描述长度**：1-2 句话，简明扼要

---

## 测试与上线清单

### 本地测试步骤

#### 1. 启动本地服务器

```bash
npm run dev
```

访问: `http://localhost:8080/pages/reports/{reportId}/`

#### 2. 语言切换测试

**测试所有三种语言**：

- [ ] 点击导航栏语言切换器 → 中文
  - [ ] 导航栏显示"首页"、"趋势研究"
  - [ ] 页脚显示中文
  - [ ] 报告内容显示中文
  - [ ] 页面标题（浏览器标签）显示中文

- [ ] 切换到 English
  - [ ] 导航栏显示"Home"、"Trend Research"
  - [ ] 页脚显示英文
  - [ ] 报告内容显示英文
  - [ ] 页面标题显示英文

- [ ] 切换到 العربية
  - [ ] 导航栏显示阿拉伯文
  - [ ] 页脚显示阿拉伯文
  - [ ] 报告内容显示阿拉伯文
  - [ ] 页面布局变为RTL（从右到左）
  - [ ] 页面标题显示阿拉伯文

#### 3. 跨页面语言持久化测试

- [ ] 在报告页选择英文
- [ ] 点击导航栏"Trend Research"跳转到趋势页
- [ ] 趋势页应显示英文
- [ ] 点击报告卡片返回报告页
- [ ] 报告页应保持英文

#### 4. 图片优化测试

- [ ] 检查 `images/optimized/` 目录存在
- [ ] 检查每张图片都有对应的WebP版本
- [ ] 打开浏览器开发者工具 → Network
- [ ] 刷新页面，检查加载的是 `.webp` 文件
- [ ] 检查控制台无 404 错误

#### 5. 响应式测试

**桌面端**：
- [ ] 1920x1080 正常显示
- [ ] 1366x768 正常显示

**平板端**：
- [ ] 768x1024 正常显示
- [ ] 导航栏适配良好

**移动端**：
- [ ] 375x667 (iPhone SE) 正常显示
- [ ] 汉堡菜单可用
- [ ] 滚动流畅

#### 6. 趋势页索引测试

- [ ] 访问 `http://localhost:8080/pages/trends/`
- [ ] 新报告卡片显示在最前面
- [ ] 卡片信息正确（标题、副标题、描述、标签）
- [ ] 封面图正确显示
- [ ] 点击卡片能跳转到报告页

#### 7. 控制台检查

**打开浏览器开发者工具 → Console**：

- [ ] 无 JavaScript 错误
- [ ] 无 404 资源加载错误
- [ ] 看到成功日志：
  ```
  ✅ 导航栏翻译数据已注册
  ✅ 页脚翻译数据已注册
  ✅ i18n 系统初始化成功
  ✅ 报告页面 i18n 初始化完成
  ```

### 上线前最终检查

#### 代码检查

- [ ] 所有文件已保存
- [ ] 图片已优化（运行过 `npm run optimize-images:report`）
- [ ] `reports-index.json` 已更新
- [ ] Git 提交信息清晰

#### 提交到Git

```bash
# 查看变更
git status

# 添加新文件
git add pages/reports/{reportId}/

# 添加索引文件
git add pages/trends/data/reports-index.json

# 提交
git commit -m "Add new report: {reportId} - {报告标题}

- 添加三语言翻译（zh-CN, en-US, ar-SA）
- 优化图片（WebP格式）
- 更新报告索引

🤖 Generated with Claude Code"

# 推送
git push
```

#### Vercel 部署检查

- [ ] 推送后访问 Vercel 控制台
- [ ] 等待构建完成
- [ ] 访问生产环境 URL
- [ ] 重复所有本地测试步骤
- [ ] 特别测试：硬刷新 (Cmd/Ctrl + Shift + R) 确保缓存清除

---

## 常见错误与排查

### 错误 1: 导航栏/页脚翻译丢失

**症状**：
- 导航显示空白或英文键名（如 "nav.home"）
- 页脚显示空白

**原因**：
- `getI18nData()` 未正确合并导航/页脚翻译
- 或者翻译文件未导出到 `window`

**解决**：
1. 检查 `script.js` 中的 `getI18nData()` 函数
2. 确认包含导航和页脚翻译合并代码
3. 检查 `i18n/zh-CN.js` 等文件末尾有 `window.zhCN = zhCN;`

### 错误 2: 脚本加载顺序错误

**症状**：
- 控制台报错："window.i18n is undefined"
- 翻译完全不工作

**原因**：
- HTML中脚本加载顺序错误

**解决**：
严格按照以下顺序加载：
1. image-optimizer-loader.js
2. i18n-core.js + rtl-handler.js
3. Navigation/i18n.js
4. 页面 i18n/*.js
5. Navigation/script.js + Footer/script.js
6. script.js

### 错误 3: 图片未优化

**症状**：
- 浏览器加载的是 PNG/JPG 而非 WebP
- `images/optimized/` 目录不存在

**原因**：
- 未运行 `npm run optimize-images:report`

**解决**：
```bash
npm run optimize-images:report
```

### 错误 4: 路径 404 错误

**症状**：
- 控制台大量 404 错误
- 样式/脚本加载失败

**原因**：
- 相对路径错误（应该是 `../../../`）

**解决**：
检查 HTML 中所有路径，从报告目录到根目录需要三层 `../`

### 错误 5: 报告未出现在趋势页

**症状**：
- 趋势页看不到新报告

**原因**：
- 未更新 `reports-index.json`
- 或 JSON 格式错误

**解决**：
1. 检查 `pages/trends/data/reports-index.json`
2. 验证 JSON 格式正确（使用 JSONLint）
3. 确认新报告在 `reports` 数组开头

### 错误 6: 语言切换后页面空白

**症状**：
- 切换到某个语言后内容消失

**原因**：
- 对应语言的翻译文件缺失字段
- `updatePageContent()` 访问了不存在的数据

**解决**：
1. 对比三个语言文件的对象结构
2. 确保完全一致
3. 在 `updatePageContent()` 中添加空值检查

### 错误 7: RTL布局错乱

**症状**：
- 阿拉伯语时布局混乱

**原因**：
- CSS未添加 RTL 支持

**解决**：
在 `style.css` 中为关键元素添加：
```css
[dir="rtl"] .your-element {
    /* RTL样式 */
}
```

---

## 附录：完整示例检查清单

### 生成新报告前确认

- [ ] 已阅读并理解本文档所有章节
- [ ] 用户已提供完整的文字报告内容
- [ ] 确认报告元数据（年份、月份、地区）
- [ ] 确认报告ID格式正确

### 文件生成检查

- [ ] `i18n/zh-CN.js` - 结构完整，已导出到 window
- [ ] `i18n/en-US.js` - 结构完整，已导出到 window
- [ ] `i18n/ar-SA.js` - 结构完整，已导出到 window
- [ ] `index.html` - 占位符正确，脚本顺序正确
- [ ] `script.js` - 包含所有必需函数，i18n集成正确
- [ ] `style.css` - 包含基本样式，RTL支持
- [ ] `images/` - 图片已放置（如有）

### 集成检查

- [ ] `reports-index.json` 已更新
- [ ] 新报告数据在数组开头
- [ ] 所有路径正确
- [ ] JSON格式有效

### 测试检查

- [ ] 本地服务器启动成功
- [ ] 三种语言切换正常
- [ ] 跨页面语言持久化正常
- [ ] 图片优化成功
- [ ] 响应式正常
- [ ] 趋势页索引正常
- [ ] 控制台无错误

### 上线检查

- [ ] Git 提交完成
- [ ] Vercel 构建成功
- [ ] 生产环境测试通过

---

## 结语

这份指南是 YECO 网站的"宪法"，每一条规范都基于系统的实际架构。严格遵循本指南，可以确保新报告完美集成，无需调试和修bug。

**记住核心原则**：
1. 脚本顺序不可变
2. 翻译数据必须合并
3. 三语言结构必须一致
4. 图片只放 `images/` 目录
5. 索引必须更新

如有疑问，请参考现有报告：`pages/reports/2025-10-middleeast/`

---

**文档版本**: 1.0
**最后更新**: 2025-11-13
**维护者**: YECO Team
