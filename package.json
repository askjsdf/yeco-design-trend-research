{
  "name": "yeco-website",
  "version": "1.0.0",
  "description": "YECO - 世界文化交融的香水包装设计专家",
  "private": true,
  "scripts": {
    "optimize-images": "node scripts/optimize-images.js",
    "optimize-images:report": "node scripts/optimize-images.js --path pages/reports",
    "build": "npm run optimize-images",
    "dev": "npx http-server -p 8080 -c-1"
  },
  "dependencies": {
    "sharp": "^0.33.0",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
